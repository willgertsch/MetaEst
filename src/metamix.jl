function metamix(
    distribution::AbstractChar,
    data::Vector{T},
    g::Int,
    alg::AbstractChar = "ECA",
    swarm_size::Int = 100,
    max_iter::Int = 1000,
    parameters::Vector{T},
    bounds::Matrix{T}
) where T <: AbstractFloat
    
    # set up mixturemod data structure

    # switch between algorithm choices

    # update stored data

    # return gaussmixmod object
end